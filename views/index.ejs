<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Mini Message Board</title>
    <link rel="stylesheet" href="/css/styles.css" />
  </head>
  <body>
    <header>
      <h1><%= title %></h1>
      <a href="/new">New Message</a>
    </header>
    <ul>
      <% messages.forEach((message) => { %>
        <li>
          <h3>User: <%= message.username %></h3>
          <p>Text: <%= message.message %></p>
          <p>Date:
            <%= message.added.toLocaleString("en-US", {
              year: "numeric",
              month: "long",
              day: "numeric",
              hour: "2-digit",
              minute: "2-digit"
            }) %>
          </p>

          <a href="/messages/<%= message.id %>">Message Details</a>
        </li>
      <% }); %>
    </ul>
  </body>
</html>
